package restaurant;

import java.util.ArrayList;
import java.util.Date;

public class Menu {
    //declare two private fields, menuItems (ArrayList<MenuItem>) and lastUpdated(Date)
    private ArrayList<MenuItem> menuItems = new ArrayList<>();
    private Date lastUpdated; //no value for defining class

    //no constructor definition - allow for default constructor
}
